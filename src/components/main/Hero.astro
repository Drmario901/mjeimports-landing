---
import { Plane, Check, ArrowRight, Package, Truck, Shield, Clock } from '@lucide/astro';
import { Picture } from "astro:assets";

import DeskHero from "../../assets/desk-hero.webp";
import ResHero from "../../assets/res-hero.webp";

import FlagUSA from "../../assets/flags/usa.png";
import FlagVE from "../../assets/flags/venezuela.png";
---

<link
  rel="preload"
  fetchpriority="high"
  as="image"
  href={DeskHero.src}
/>
<link
  rel="preload"
  fetchpriority="high"
  as="image"
  media="(max-width: 768px)"
  href={ResHero.src}
/>

<style>
  html {
    scroll-behavior: smooth;
  }

  .reveal {
    opacity: 0;
    transform: translateY(40px);
    transition: all 0.8s ease-out;
  }
  .reveal.active {
    opacity: 1;
    transform: translateY(0);
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    background-size: cover;
    background-position: center;
    z-index: 0;
    transition: opacity 0.4s ease;
  }

  .hero-desktop { display: block; }
  .hero-mobile { display: none; }

  @media (max-width: 768px) {
    .hero-desktop { display: none; }
    .hero-mobile { display: block; }
  }
</style>

<section id="inicio" class="relative text-white py-24 px-6 overflow-hidden">

  <div
    class="hero-bg hero-desktop"
    style={`background-image: url(${DeskHero.src});`}
    aria-hidden="true"
  ></div>

  <div
    class="hero-bg hero-mobile"
    style={`background-image: url(${ResHero.src});`}
    aria-hidden="true"
  ></div>

  <div class="absolute inset-0 bg-gradient-to-b from-[#002f6c8c] to-[#002f6cbf] z-1"></div>

  <div class="absolute inset-0 opacity-10 z-0" aria-hidden="true">
    <div class="absolute top-20 left-10 animate-float">
      <Package size={40} class="text-white/20" />
    </div>
    <div class="absolute top-40 right-20 animate-float-delayed">
      <Plane size={32} class="text-white/20" />
    </div>
    <div class="absolute bottom-20 left-1/4 animate-float">
      <Package size={24} class="text-white/20" />
    </div>
  </div>

  <div class="max-w-7xl mx-auto flex flex-col lg:flex-row items-center justify-between gap-12 relative z-10">

    <div class="order-1 lg:order-none text-center lg:text-left space-y-8 max-w-2xl">

      <h1 class="text-4xl sm:text-5xl font-bold leading-tight reveal">
        Traemos tus productos desde 
        <span class="inline-flex items-center gap-3 text-[#2ad37a]">
          USA
          <Picture 
            src={FlagUSA}
            alt="Bandera de Estados Unidos"
            width={40}
            height={24}
            formats={['webp']}
            loading="eager"
            class="h-10 w-auto object-contain"
          />
        </span>

        <span class="inline-flex items-center gap-3 text-[#2ad37a]">
          hasta Venezuela
          <Picture 
            src={FlagVE}
            alt="Bandera de Venezuela"
            width={40}
            height={24}
            formats={['webp']}
            loading="eager"
            class="h-10 w-auto object-contain"
          />
        </span>
      </h1>

      <p class="text-lg sm:text-xl opacity-90 reveal">
        Compra en tus tiendas favoritas y recibe perfumes, tecnología, relojes, ropa, repuestos ¡y mucho más!
      </p>

      <p class="text-lg sm:text-xl opacity-90 reveal font-bold">
        Fácil, rápido y sin complicaciones.
      </p>

      <ul class="bg-white/10 backdrop-blur-sm rounded-2xl p-6 border border-white/10 reveal space-y-4 text-white/90">
        <li class="flex gap-3 justify-center lg:justify-start">
          <div class="w-6 h-6 bg-[#2ad37a] rounded-full flex justify-center items-center">
            <Check size={14} class="text-white" />
          </div>
          <span>Envíos aéreos semanales y marítimos mensuales</span>
        </li>
        <li class="flex gap-3 justify-center lg:justify-start">
          <div class="w-6 h-6 bg-[#2ad37a] rounded-full flex justify-center items-center">
            <Check size={14} class="text-white" />
          </div>
          <span>Solo 50% de adelanto para encargar tu producto</span>
        </li>
        <li class="flex gap-3 justify-center lg:justify-start">
          <div class="w-6 h-6 bg-[#2ad37a] rounded-full flex justify-center items-center">
            <Check size={14} class="text-white" />
          </div>
          <span>Seguimiento personalizado de tu pedido</span>
        </li>
        <li class="flex gap-3 justify-center lg:justify-start">
          <div class="w-6 h-6 bg-[#2ad37a] rounded-full flex justify-center items-center">
            <Check size={14} class="text-white" />
          </div>
          <span>Precios justos y entrega garantizada</span>
        </li>
      </ul>

      <div class="flex flex-col sm:flex-row justify-center lg:justify-start gap-4 pt-6 reveal">
        <a
          href="#servicios"
          class="group bg-[#2ad37a] text-[#002f6c] hover:bg-[#25b86a] hover:scale-105 transition-all duration-300 font-semibold px-6 py-3 rounded-xl shadow-lg hover:shadow-xl flex items-center justify-center"
        >
          <span>Ver Servicios</span>
          <ArrowRight size={18} class="ml-2 group-hover:translate-x-1 transition-transform" />
        </a>

        <a
          href="#contacto"
          class="group border-2 border-white text-white hover:bg-white hover:text-[#002f6c] hover:scale-105 transition-all duration-300 font-semibold px-6 py-3 rounded-xl shadow-lg hover:shadow-xl flex items-center justify-center"
        >
          <Plane size={18} class="mr-2 group-hover:rotate-12 transition-transform" />
          <span>Solicitar Encargo</span>
        </a>
      </div>
    </div>

    <div class="order-2 lg:order-none w-full max-w-md grid grid-cols-1 sm:grid-cols-2 gap-6">

      <div class="backdrop-blur-md bg-white/10 rounded-2xl p-8 border border-white/20 transition-all duration-300 reveal hover:bg-white/15 shadow-md">
        <div class="text-center space-y-4">
          <div class="w-16 h-16 bg-[#2ad37a] rounded-full flex items-center justify-center mx-auto">
            <Truck size={32} class="text-white" />
          </div>
          <h3 class="font-semibold text-base sm:text-lg">Envío Rápido</h3>
          <p class="text-sm opacity-80">Entregas semanales</p>
        </div>
      </div>

      <div class="backdrop-blur-md bg-white/10 rounded-2xl p-8 border border-white/20 transition-all duration-300 reveal hover:bg-white/15 shadow-md">
        <div class="text-center space-y-4">
          <div class="w-16 h-16 bg-[#002f6c] rounded-full flex items-center justify-center mx-auto">
            <Shield size={32} class="text-white" />
          </div>
          <h3 class="font-semibold text-base sm:text-lg">100% Seguro</h3>
          <p class="text-sm opacity-80">Garantía total</p>
        </div>
      </div>

      <div class="backdrop-blur-md bg-white/10 rounded-2xl p-8 border border-white/20 transition-all duration-300 reveal hover:bg-white/15 shadow-md">
        <div class="text-center space-y-4">
          <div class="w-16 h-16 bg-[#2ad37a] rounded-full flex items-center justify-center mx-auto">
            <Package size={32} class="text-white" />
          </div>
          <h3 class="font-semibold text-base sm:text-lg">Seguimiento</h3>
          <p class="text-sm opacity-80">Estatus en tiempo real</p>
        </div>
      </div>

      <div class="backdrop-blur-md bg-white/10 rounded-2xl p-8 border border-white/20 transition-all duration-300 reveal hover:bg-white/15 shadow-md">
        <div class="text-center space-y-4">
          <div class="w-16 h-16 bg-[#002f6c] rounded-full flex items-center justify-center mx-auto">
            <Clock size={32} class="text-white" />
          </div>
          <h3 class="font-semibold text-base sm:text-lg">24/7</h3>
          <p class="text-sm opacity-80">Soporte continuo</p>
        </div>
      </div>

    </div>
  </div>
</section>

<script>
  const revealElements = document.querySelectorAll(".reveal");
  const revealOnScroll = () => {
    const triggerBottom = window.innerHeight * 0.85;
    revealElements.forEach(el => {
      if (el.getBoundingClientRect().top < triggerBottom) {
        el.classList.add("active");
      }
    });
  };
  window.addEventListener("scroll", revealOnScroll);
  revealOnScroll();
</script>
